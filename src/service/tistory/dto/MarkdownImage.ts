export class MarkdownImage {
  private static LOCAL_IMAGE_REGEX = new RegExp('^(http|https)://', 'i');

  private readonly filePath: string;
  private urlPath: string;

  // get isLocalImage(): boolean {
  //
  // }
}
